rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Keep classroom demo collections closed until a specific tool needs access.
    match /wordcloud_words/{document} {
      allow read, write: if false;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
